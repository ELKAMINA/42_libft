#include "../libft.h"

void	ft_lstclear(t_list **lst, void (*del) (void	*))
{
	t_list	*after;
	t_list	*actual;

		if	(!lst || !del)
			return ;
		actual = *lst;
		while (*lst != NULL)
		{
			next = *actual -> next;
			ft_lstdelone(*actual, del);
			*actual = after;
		}
		*lst =  NULL;
}
